#include <cassert>

#include "../Basics/operators.hpp"

int main(void) {
  LadderOp op1(0, true);
  LadderOp op2(1, true);
  Monomial mn1(op1);
  Monomial mn2(op2);
  Polynomial pl1(mn1);
  std::cout << "pl1 = " << pl1.toString() << std::endl;
  return EXIT_SUCCESS;
}
